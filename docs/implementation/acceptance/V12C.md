# V12C Acceptance

- [x] One `ADMIN_PASSWORD_MIN_LENGTH` validator accepts 6 and rejects 5 for bootstrap/reset.
- [x] Changing `.env` alone does not mutate the database and dev does not auto-bootstrap.
- [x] `npm run admin:reset-password` uses hidden confirmation, bcrypt, existing-user update, audit, clean pool shutdown and no password output.
- [x] Safe environment automation is optional and does not print secrets.
- [x] Development limiter defaults to 60s/20 failures; production to 300s/10; invalid values fail startup.
- [x] Failed login increments, success clears, reset clears supported in-process entries and restart clears memory state.
- [x] HTTP 429 supplies valid Retry-After without account enumeration.
- [x] Login shows an accessible seconds countdown, preserves fields, disables/re-enables submit and distinguishes invalid credentials.
- [x] Vite uses port 5173 with `strictPort=true`.
- [x] Environment examples and deployment docs contain no real credentials.
- [x] Unit, integration, E2E and required commands pass.
