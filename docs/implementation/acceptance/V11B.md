# V11B Acceptance

- [x] Login visibly shares Cô Vy branding, includes “← Về trang chủ”, privacy guidance and remains scrollable with the mobile keyboard.
- [x] Email/password fields use semantic names and autocomplete; password visibility control has a changing accessible label.
- [x] Submit loading, disabled state, Enter submission and friendly Vietnamese error mapping work.
- [x] Remember selected stores exactly one token in localStorage and remembers email/preference.
- [x] Remember unselected stores exactly one token in sessionStorage and does not persist the session across browser restarts.
- [x] Password is absent from localStorage, sessionStorage, IndexedDB, client-readable cookies, configuration, logs and reports.
- [x] AuthContext/API client use centralized auth storage; logout/401/invalid bootstrap clear both token stores.
- [x] Valid `/api/auth/me` restores auth; protected routes wait; intended destination forwarding remains intact.
- [x] Authenticated `/admin/login` redirects to `/admin` without a loop.
- [x] Login has no horizontal overflow at 360px and required screenshots/tests/commands pass.
