# Homepage content, contact links and local media paths live in client/src/content/publicHome.ts.
VITE_API_BASE_URL=http://localhost:4000

# Optional. Homepage falls back to a local location card when omitted.
VITE_GOOGLE_MAPS_EMBED_API_KEY=
